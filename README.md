# obs-audio-peak-notification
A OBS script to send a notification to my phone that audio is peaking or in my case that my dog is barking 

## Package Install

``` bash
pip install -r requirements.txt
```
